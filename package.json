{
  "name": "hopify-kosova",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @hopify/web dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "db:seed": "tsx prisma/seed.ts"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}
